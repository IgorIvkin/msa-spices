package com.igorivkin.msaspices.lib.convert;

/**
 * Represents type of conversion to string for a service that prepares values
 * to be logged. Currently, supports JSON-conversion.
 */
public enum LogConversionType {
    JSON,
    XML;
}
